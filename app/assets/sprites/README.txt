备用三视图立绘（正面 / 侧面 / 背面，各 3 个尺寸 + 图标）。

当前版本的桌宠只用 `assets/pet-character.png` 单张立绘，这个目录暂时没有被代码引用，
是早期「行走 / 转向动画」版本留下的素材，先留着以免丢失。

要用的话：在 `renderer/renderer.js` 的 `loadArt()` 里改成按状态取对应图片即可
（`_187 / _238 / _306` 是同一姿态的不同缩放尺寸）。
