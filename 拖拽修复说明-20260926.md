# 拖拽修复说明 · 2026-09-26

> 版本：v0.1（`app/package.json` 0.1.0）
> 主题：**桌宠拖拽时立绘偏出光标**的最终修复

---

## 一、症状（用户视角）

按时间顺序出现的现象，最后收敛到一句：

| # | 现象 | 是否本次修复 |
|---|---|---|
| 1 | 拖拽跟不上鼠标 | 部分（见下） |
| 2 | 按住不拖会「平移」 | 已修（1px 死区） |
| 3 | 拖完松手后点不了（戳不出反应） | 已修（点穿改包围盒） |
| 4 | **往右拖、立绘往右偏，偏出光标** | ✅ **本次根因** |
| 5 | 复杂/快速运动后立绘偏离光标，点一下又弹回正确位置 | ✅ **本次根因** |

关键特征：
- 偏移发生在**拖拽过程中**（不是松手后）；
- **窗口**其实一直是准的，偏的是**立绘**；
- 「点一下弹回」= 重新抓取时布局重排，偏移被重置。

---

## 二、根因（实测定位）

### 结论

在这台机器上（**125% DPI + `transparent: true` 透明窗口**），
`BrowserWindow.setPosition()` / 移动窗口时**窗口尺寸会随位移持续变大**：

```
width  += dx / 2      （横向移动时）
height += dy / 2 ~ 2dy/3（纵向移动时）
```

而立绘的 CSS 是 **`#pet { margin: 0 auto }` 水平居中**（见 `app/renderer/style.css`），
**窗口一变宽 → 立绘在窗口内被重新居中 → 立绘相对光标右移**。
窗口每 tick 变宽一点，立绘就一点一点偏出去；重新抓取时布局复位 → 「弹回」。

拖拽的坐标公式自始至终是**对的**，所以只盯 `target/afterError` 永远看不出问题——
**那测的是窗口，不是立绘**。

### 决定性证据

隔离实验（无拖拽、无 IPC、纯粹调 `setPosition`，`dragging:false`）：

```
[bounds-diag] {"w":384,"h":196,"x":602,...,"dragging":false}
[bounds-diag] {"w":434,"h":196,"x":708,...,"dragging":false}
[bounds-diag] {"w":558,"h":196,"x":866,...,"dragging":false}   ← 窗口被自己撑大了
```

三者互相印证，排除了「只是 API 读数抖动」：

```
getBounds()        = {x:866, width:558}
getSize()          = [558, 196]
getContentBounds() = {x:866, width:558}
```

同时：
- 单显示器（`1536×864` DIP / `1920×1080` physical，scale = 1.25），排除跨屏 DPI；
- 整个拖拽过程 **`pet:resize` 一次都没被调用**（`[resize-diag]` 无输出），
  说明尺寸不是应用代码显式改的，而是移动窗口的副作用。

### 为什么以前没发现

`[drag-diag]` 里的 `target / before / after / afterError` 全都正常（`afterError ≤ 1px`），
因为窗口确实被摆到了正确位置。**立绘是否跟着窗口走**，没有任何指标覆盖。

---

## 三、改动清单

### 1) 拖拽架构：事件直驱 → 主进程 8ms 自采样

| 文件 | 改动 |
|---|---|
| `app/main.js` | `dragStep` 改由 `setInterval(dragStep, 8)` 驱动；`beginDrag` 启动、`endDrag` 关闭定时器；删掉 `drag-tick` 的 IPC 处理器 |
| `app/renderer/renderer.js` | `mousemove` 不再发 `dragTick()`（只保留 `dragMoved` 用于区分「拖」还是「点」）；`mousedown→dragStart`、`mouseup→dragEnd` 不变 |
| `app/preload.js` | 移除 `dragTick` |

理由：原来 `mousemove → drag-tick IPC → setPosition` 会形成反馈环——
移动窗口打断 `mousemove`，导致 `drag-tick` 断流（实测 `gap` 高达 **1401ms**），表现为拖拽一顿一顿、落后鼠标。
改为主进程自己按 8ms 采样光标后，`gap` 稳定在 **8–9ms**，不再依赖 mousemove。

### 2) 尺寸钉死（本次根因修复）

`app/main.js`：

```js
const PET_W = 380;   // 窗口固定宽度
let petH = 196;      // 期望高度，由 pet:resize 维护

// 拖拽定位：用 setBounds 并把 w/h 每 tick 覆盖成固定值
petWin.setBounds({ x: tx, y: ty, width: PET_W, height: petH });
```

要点：
- **必须每 tick 显式覆盖 w/h**。若回填当前 bounds（`height: cur.height`），
  会把已经变大的尺寸又写回去，形成自增累积（实测改宽度后高度又开始涨）。
- `pet:resize` 里同步维护 `petH`，并过滤渲染层瞬时上报的 4px 噪声（`if (h >= 60) petH = h`）。

### 3) 保留 1px 死区

```js
if (Math.abs(beforeErrX) <= 1 && Math.abs(beforeErrY) <= 1) return;
```

125% DPI 下 `setBounds/getPosition` 有 ±1px 取整误差，不抑制会形成
「窗口被来回纠正」的高频抖动，观感就是**按住不动却一直平移**。

### 4) 测试工具链

| 文件 | 说明 |
|---|---|
| `app/vendor/input/Input.cs` | 新增 `press` / `release` / `fdrag`（高速直线，默认 200 步 / 1ms）/ `fcircle`（高速画圆） |
| `app/vendor/input/input.exe` | 上述命令重新编译产物（vendor 可执行文件按 `.gitignore` 白名单入库） |
| `test-drag.ps1` | 常速用例：单方向 / 复杂 L 形 / 圆形 / 长时 + 按住不动防漂移；每段前后快照 `position.json` |
| `test-fast.ps1` | 高速用例：四个方向高速直线 + 高速圆形 |
| `shots/drag-verify.png` | 视觉验证：在计算出的抓取点上画红十字，确认立绘正好压在准星下 |

---

## 四、验证结果

环境：`1536×864` DIP / 125%，窗口自 `(600,300)` 出发。

### 高速用例（`test-fast.ps1`）

| 测试 | 起点 → 终点 | 位移 | 判定 |
|---|---|---|---|
| F1 快速右移 | 600,300 → 1060,300 | +460 | ✓（预期 0.30×1536=461） |
| F2 快速左移 | 1060,300 → 598,300 | −462 | ✓ |
| F3 快速下移 | 598,300 → 597,558 | +258 | ✓（预期 0.30×864=259） |
| F4 快速上移 | 597,558 → 596,300 | −258 | ✓ |
| F5 高速圆形 | 596,300 → 596,300 | 净 0 | ✓ |

全程窗口尺寸**稳定不变**（`w = 380/381`，`h = 194/196`，±1 为 DPI 取整）。

### 跟踪质量（`[drag-diag]`）

```
gap   : 8–9ms     （修复前事件直驱时出现过 16 / 273 / 451 / 534 / 1401ms 的断流）
step  : 1.5–2.4ms （单次 dragStep 耗时，主进程不卡）
afterError : ≤ 1px（setBounds 立即生效）
sp    : [119, 0, 142, 189.32] 恒不变（立绘在窗口内没有移位）
```

### 视觉验证

`shots/drag-verify.png`：拖到 `(902,300)` 后截屏，在 `窗口 + (190,130)`（= 抓取点）画红十字，
立绘正好落在准星下（身体/围裙处，水平居中）。

---

## 五、遗留 / 后续

- 诊断日志（`[drag-diag]` / `[hit-diag]` / `[pointer-diag]`）**按用户要求保留**，观察一段时间确认无复发后再删。
- `solidAtCursor()` 目前是**立绘包围盒**判定（非逐像素 alpha）：
  逐像素会把立绘身上约 31% 的透明缝也判成穿透，导致「点不中」。包围盒换取可点性，代价是立绘四角空白也会接鼠标。
- 拖拽中每 tick 都用固定 w/h 覆盖，若将来窗口宽度不再固定（如允许用户缩放窗口），需同步改为读取目标尺寸。
