# 大肥鱼桌宠 · 立绘交接说明

## 一、要改的只有一个文件

```
app/assets/pet-character.png      ← 桌宠立绘
```

**直接替换成同名文件即可，不需要改任何代码。**

## 二、技术要求

| 项目 | 要求 |
|---|---|
| 格式 | **PNG，背景必须透明**（桌宠是直接浮在桌面上的，不能有白底/色块） |
| 尺寸 | 高度建议 **≥ 700px**；程序按**宽度**缩放显示（默认 350px 宽） |
| 构图 | **人物居中、底部贴住画面底边**（说话气泡会显示在立绘下方，别挡住脸） |
| 比例 | 偏竖构图更好（当前占位素材约 1040×875，偏方） |
| 空边 | 四周留少量透明边距即可，不要留大片空白 |

## 三、形象要求（二创「吃白饭的大肥鱼」）

参考 `ref/` 目录里的三张图。核心要素：

- **蓝色渐变长卷发**
- **鲸鱼鳍状耳饰**（头两侧的鱼鳍）
- **白色荷叶边女仆头饰**
- **深蓝女仆裙 + 白色围裙**（围裙上有**鲸鱼图案**）+ **金色裙摆**
- **鲸鱼尾巴**
- **大蓝眼睛**
- 画风：**二次元插画风**（不是扁平卡通）

气质：傲娇、嘴硬心软的女仆。

## 四、可选加分项：表情差分

程序目前只读一张 `pet-character.png`。如果想做多表情（待机 / 开心 / 害羞 / 惊慌 / 生气），
请按下面命名交付，并告诉开发者，会加一段表情切换逻辑：

```
app/assets/pet-idle.png       待机
app/assets/pet-happy.png      开心
app/assets/pet-shy.png        害羞
app/assets/pet-panic.png      惊慌
app/assets/pet-angry.png      生气
```

同样要求：PNG 透明底、尺寸一致、人物位置对齐（切换时不跳动）。

## 五、改完怎么用

1. 替换 `app/assets/pet-character.png`
2. 在 `app` 目录执行重新打包：
   ```
   npm run dist
   ```
   产物在 `app/dist/` 下的安装包，双击覆盖安装即可。

（开发调试可直接 `npm start`，改完重启就生效。）
