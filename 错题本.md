# 错题本

> 记录排查过程中**走过的弯路、误判和踩过的坑**，避免重复犯。
> 格式：现象 → 当时的判断 → 实际真相 → 以后怎么做。

---

# 2026-09-25 / 26 · 桌宠拖拽「立绘偏出光标」

**最终根因**：`setPosition` 移动窗口时窗口尺寸会随之变大（125% DPI + 透明窗口），
立绘 `margin: 0 auto` 居中 → 窗口变宽 → 立绘在窗口内右移 → 偏出光标。
详见 `拖拽修复说明-20260926.md`。

下面是为找到它付出的代价。

---

## 错题 1：先怀疑坐标公式（其实它一直是对的）

- **现象**：拖拽时立绘跟不上/偏离光标。
- **当时判断**：`target = dragWin + (cursor - dragAnchor)` 的坐标空间可能错了
  （DIP / 物理像素混用），于是反复折腾公式。
- **真相**：公式没问题。`[drag-diag]` 里 `afterError` 恒 ≤ 1px，
  说明窗口每次都被摆到了目标位置。
- **以后**：先用一条日志**证明公式对不对**（`target - 实际位置`），
  再决定要不要动公式。`afterError ≤ 1px` 就是「公式无罪」的铁证。

## 错题 2（最贵的一题）：只测窗口，不测立绘

- **现象**：用户说的是「**立绘**偏出光标」。
- **当时判断**：加了一堆 `target / before / after / afterError / cursor` 日志，
  反复看窗口坐标，「一切正常」，于是得出「大概是视觉暂留/延迟」的结论。
- **真相**：**我测的是窗口，用户看的是立绘**。窗口一直正确，立绘一直在飘。
  两件事在「窗口尺寸恒定」的假设下等价，而这个假设恰恰是错的。
- **以后**：**先测用户真正看到的东西**。用户说立绘，就去量立绘的屏幕坐标
  （`window.screenX + pet.getBoundingClientRect().left`），而不是它的容器。
  「指标全绿但问题依旧」= 指标选错了，不是问题玄学。

## 错题 3：把「断流」当成最终病因（真问题，但不是这个问题的答案）

- **现象**：拖拽一顿一顿、落后鼠标。
- **当时判断**：`mousemove → drag-tick IPC → setPosition` 形成反馈环，
  移动窗口打断了 `mousemove`，`drag-tick` 断流。
- **真相**：断流**确实存在**（实测 `gap` 高达 1401ms），改成主进程 8ms 自采样后
  `gap` 降到 8–9ms —— 但**立绘偏移依旧**。断流是另一个（真实的）问题。
- **以后**：一个现象可能同时有多个成因。修完一个要**重新验证原症状**，
  不能因为「这个 bug 是真的」就宣布问题解决。

## 错题 4：用 `setPointerCapture` + pointer events 换掉 mouse events

- **现象**：想用 pointer 事件统一鼠标/触摸。
- **当时判断**：pointer 事件更现代，且能拿到 `screenX/screenY` 直接算锚点。
- **真相**：`setPointerCapture` 把 `pointermove` 吞在元素上，`window` 上的
  `pointermove` 不再触发 → `drag-tick` 一次都没发 → **拖拽彻底失效**，
  还被 Windows 原生拖拽接管（表现为「立绘偏出光标」的另一种错觉）。
- **以后**：动「输入事件类型」这种地基改动，先做**最小可运行验证**
  （能不能收到 move、能不能拖动），别一次性换完再测。

## 错题 5：去掉 1px 死区（换来「按住就平移」）

- **现象**：想让窗口更精确地贴住光标，去掉死区，改成「不相等就 set」。
- **当时判断**：死区是多余的，精确Follow更好。
- **真相**：125% DPI 下 `setPosition/getPosition` 有 ±1px 取整误差，
  没有死区就会**每 tick 反复纠正 1px**，观感就是「按住不拖也一直平移」。
- **以后**：DPI 缩放下，**取整误差是常态**，任何「绝对相等」的判定都要留容差。
  死区不是偷懒，是必需品。

## 错题 6：重复注册 IPC 处理器，把诊断日志覆盖成空

- **现象**：诊断日志显示「步数:0」，但窗口确实动了。
- **当时判断**：以为拖拽没触发、或者日志写错地方。
- **真相**：`ipcMain.on('drag-start' / 'drag-tick' / 'drag-end')` 存在**两套注册**，
  第二套的 `endDrag` 用空数据覆盖了诊断日志。
- **以后**：诊断日志对不上时，先怀疑**同一事件被注册了多次**（`ipcMain.on` 是叠加的，
  不是覆盖）。`grep -n "ipcMain.on('xxx'"` 数一遍。

## 错题 7：诊断本身有性能开销，也会改变被测对象

- **现象**：拖拽时 `step` 偶发偏大。
- **当时判断**：主进程卡了。
- **真相**：每 tick 调 `getBounds()` + `getSize()` + `getContentBounds()` 纯属自找开销。
- **以后**：诊断要**分级**：轻量（计数器、误差）常开；重量（多次系统调用、dump 大对象）临时开、用完就删。
  观测手段本身会影响观测结果。

## 错题 8：点穿用「逐像素 alpha」，把立绘判成筛子

- **现象**：点桌宠经常点不中（戳不出反应）。
- **当时判断**：应该按 alpha 精确判定「点在实心像素上」。
- **真相**：立绘身上约 **31% 是透明缝**（高光、发丝间隙），逐像素判定把这些缝也当穿透。
- **以后**：命中判定要区分「不挡桌面图标」和「点得中」两个目标。
  用**包围盒**换可点性，是这里正确的取舍；精度不是越高越好，要匹配目的。

---

## 工具 / 环境坑（与业务无关，但每次都要重新踩一遍）

| 坑 | 表现 | 处理 |
|---|---|---|
| PowerShell 执行策略 | `& .\x.ps1` 报 “running scripts is disabled” | `Set-ExecutionPolicy -Scope Process Bypass -Force` |
| Windows PowerShell 5.1 编码 | `.ps1` 里的中文/`≈` 被按 GBK 误读，**报莫名其妙的语法错** | 脚本注释**纯 ASCII**；或写 UTF-8 BOM |
| PowerShell 解析怪癖 | `@($a / 1536.0, $b / 864.0)` 被错误解析成 `@($a) / (1536.0, $b)`，报 `op_Division` 找不到 | 先 `$x = $a / 1536.0` 再 `return @($x, $y)` |
| `python` 是商店占位 | 调用退出码 9009 | 用 `node` / `.bat` / `.ps1` |
| `gh` CLI 未安装 | 无法命令行建 PR | 用 git push + GitHub compare 链接，或装 gh |
| Electron 主进程 `console.log` 丢失 | `start` 启动时看不到输出 | 统一写文件日志（`%APPDATA%\dayu-pet\debug.log`） |
| CDP 合成鼠标事件 | `screenX/screenY` 是视口坐标，不能用来测屏幕坐标相关逻辑 | 用 `input.exe` 走真实 `SendInput` |
| `git commit -m` 带中文+转义引号 | 反复被 PowerShell 拆坏 | 写消息到文件，用 `git commit -F <file>` |

---

## 一句话总结

> **指标全绿但用户说还有问题 → 一定是你在测错的东西。**
> 先问「用户看到的那个像素在哪里」，再去量它。
